# mull — Territory / Map / Mode (the data north star)

> **信条**: 領土はロスなく永遠に。地図は正しく・指すだけ・作り直せる形で。
> 削るな。要約で原本を置き換えるな。意味は「モード」で与えよ。

mull が**エージェントに渡す材料**であるための、データ層の一枚絵。
これは**構造の北極星**。視覚の側には対になる文書があるが、そちらは新規投資を止めており
このリポジトリには入っていない。本書は凍結対象ではない——**むしろ位置づけが上がった**。

> **2026-08-08 の位置づけ更新**: mull の製品は GUI ではなく MCP サーフェスになった
> （STRATEGY-2026-08.md（非公開の内部文書） / CLAUDE.md §5）。
> 上図の右上「served at use-time」＝ **MCP の 12 ツールそのもの**であり、
> **本書の serving 層が製品の出口になった**。
> 「第二の脳に渡す」という言い方は「エージェントが引く」に置き換わったが、
> **5つの不変法則（§下）は一字も変わらない。**

---

## 一枚絵

```
                         ┌──────── served at use-time ────────┐
                         │  small context → LENS (今日の小モデル用) │ ──▶ AI
                         │  large context → WHOLE map / territory │     (MCP / Copy to AI)
                         └─────────────────▲──────────────────────┘
                                           │  read-only · 使い捨て可 · 再生成可
   ┌──────────────────────────── MAP（地図 = 索引）────────────────────────────┐
   │   WHAT（何について）      ·   HOW（どう = MODE）   ·   NOW（今の文脈）        │
   │                                                                            │
   │   entity nodes ── tagged spans [ produce | consume | decide |              │
   │       │                          think | research | communicate ]          │
   │       │                                                                    │
   │       └── すべてのノード/スパンは下を「指す」（event id 参照）────────┐       │
   └───────────────────────────────────────────────────────────────────┼───────┘
                                                                         │
                       back-pointers（要約で置き換えない・原本へ戻れる）  │
                                                                         ▼
   ┌──────────────────────────── TERRITORY（領土 = _raw）──────────────────────┐
   │   immutable · lossless · complete · 絶対に消さない                          │
   │   keystrokes · clipboard · window · files · audio · browser · calendar      │
   │   ◀── 捕捉の忠実度だけが「今しか取れない」資産。処理は後でやり直せる。        │
   └────────────────────────────────────────────────────────────────────────────┘
```

---

## 5つの不変法則（The Laws）

1. **領土は不変・ロスレス・完全。絶対に消さない。** 捕捉の忠実度(タイトルでなく中身、
   断片でなく全文)が、唯一「今しか取れない」資産。2026年のあなたは再キャプチャ不能。
2. **地図は領土を“指す”索引であって、“置き換える”要約ではない。** どのノードも必ず
   生イベントを参照する。要約して原本を捨てる操作は禁止。
3. **地図は使い捨て・再生成可能・バージョン付き。** 今日の下手な地図はゼロコスト——
   無傷の領土から作り直せる。だから今ショボくても貯め続けてよい。
4. **領土は時間に敏感、地図は時間に鈍感。** 投資は領土(捕捉)へ。整形/推論は地図側=
   いつでも作り直せるので、賢さを作り込みすぎない。
5. **フィルタは読み出し時の差し替えレンズ。破壊的フィルタは存在しない。** 今日の小さい
   モデル向けに「見せ方」を絞るだけ。レンズは未来に外せ、その下に完全な記録が在る。

---

## 地図の3軸 — そして欠けている決定打

| 軸 | 問い | 役割 | mull の現状（2026-08-08） |
|---|---|---|---|
| **WHAT** | 何について | entity / topic ノード(mull=Dream=アプリ、確定申告、この人) | `Entity.swift` あり。**名寄せの解像度が低い**（残課題） |
| **HOW = MODE** | どう語っているか | **姿勢/モードのタグ** | ✅ `Mode.swift` ＋ `ModeTests`。capture-time に付与される（下表の6値） |
| **NOW** | 今どこか | now-anchor | ✅ `CurrentState.swift`。MCP `whats_active_now` として公開済み |

> **更新**: 本書執筆時「MODE 軸はほぼ無い ← 核心の欠落」と書いたが、**実装された**。
> 残る欠落は **WHAT の名寄せ解像度**と、下表「既存部品への対応」の back-pointer / 再生成。

### MODE 軸（どう語っているか）が一番効く

今日の失敗(税務の解説動画は仕事か遊びか / 口述は思考かノイズか)は、全部 MODE が無いせい。
**消す代わりに、姿勢でタグ付けして意味を与える**:

| mode | 意味 | 例 |
|---|---|---|
| **produce** | 作った(著者はあなた) | コード、原稿、デザイン |
| **consume** | 受け取った(入力) | 記事、動画、音声 |
| **decide** | 選んだ/約束した | 方針転換、採否 |
| **think** | 考えた(独り言・口述・計画) | 喋りながらの製品思考 |
| **research** | 目的を持って調べた(=consume＋意図) | 競合の FX 動画 |
| **communicate** | 伝えた | メール、投稿、会議 |

- MODE は**スパン(時間域/イベント塊)に付く**。削除でなく、**重み付け・選別・配置**に使う。

> **⚠ 2026-08-09 まで、この「重み付け・選別」は実装されていなかった。**
> `Mode` は捕捉時に計算・保存されていたが、消費先は `ContextComposer`（＝配置）のみで、
> **`Selection.rank`（＝選別）は mode を一度も読んでいなかった**。
> `Mode.weight` を追加して接続済み（重みと測定上の限界は SELECTION-LAYER §4 が正本）。
> **この節が「一番効く」と書いていた軸が、1年近く検索に効いていなかった**——
> 文書が実装より先に走ると起きる典型——確認コストの低いものを確認しないまま書いた結果である。
- 共起で解ける: 「税務の解説動画 = consume だが、確定申告エンティティと共起 → **research**」「長い口述 =
  mull エンティティ下の **think**」→ **両方とも捨てずに、意味づけて置く**。
- MODE 自体も再生成可能(今日 = ルール/ヒューリスティック、明日 = LLM)。地図側なので作り直せる。

---

## mull の既存部品への対応（骨格はもう在る）

| 一枚絵の層 | 既存の実装 | 足りないもの |
|---|---|---|
| TERRITORY | `RawStore` / `_raw/<connector>/`(immutable source) | 捕捉の**忠実度**——ブラウザ本文、打鍵の断片化（§捕捉忠実度 監査 #3/#4） |
| 設計全体図(schema) | `FolderOntology`(番号フォルダ) | territory/map/mode を映す形への整理 |
| MAP: WHAT | `Entity` / `Selection`(SELECTION-LAYER) | **entity 解像度・名寄せ(Dream=mull)** ← 最大の残課題 |
| MAP: NOW | ✅ `CurrentState`(now-anchor) | — |
| MAP: HOW | ✅ `Mode` / `Signal`(capture-time 分類) | mode の精度（今はルール、後で LLM に差し替え可） |
| back-pointer | (弱い) | ノード→生イベント id の**参照を一級にする** |
| 再生成 | (無い) | 地図の**バージョン付き再生成**(領土から rebuild) |
| serving | ✅ **MCP 12ツール**（製品の出口） | 小ctx=レンズ / 大ctx=丸ごと、の切替 |

---

## 地図ノードの概念モデル（実装でなく形）

```
MapNode {
  entity:   "mull"                      // WHAT（名寄せ済み）
  spans: [
    { range: 14:00–14:40,
      mode: .produce,                   // HOW
      refs: [evt#8841, evt#8842, …],    // ← TERRITORY を指す。要約で置換しない
      confidence: 0.7,
      mapVersion: "rules-v3" }          // ← 再生成で差し替わる
  ]
  now: false                            // NOW（anchor かどうか）
}
```

要点: ノードは**意味の置き場**だが、本文は持たない。本文は領土にあり、`refs` で**指す**だけ。
だから地図を作り直しても領土は不変、領土を読めば失われた文脈はいつでも戻る。

---

## 再生成とバージョン（なぜ今ショボくていいか）

```
territory（不変）──▶ [extract pass]（ルール今 / LLM後）──▶ map vN（破棄可能）
                          ▲
                  AIが賢くなったら pass を差し替えて map vN+1 を再生成
```

- 地図は `mapVersion` を持つ。抽出器を上げたら**全地図を作り直す**(territory は触らない)。
- 今日のウィンドウタイトル由来のゴミ project は `rules-v3` の地図。未来に `llm-v1` で上書き。
  **間違った地図はコストにならない。**

---

## 使う瞬間に渡す（serving）

- **小コンテキスト(今日の ChatGPT)** → **レンズ**で地図のスライスを渡す(今回 Copy to AI の
  ①②フィルタ＝レンズの一例)。レンズは破壊しない・差し替え可能。
- **大コンテキスト(未来 / 接続エージェント)** → 地図ごと、必要なら領土ごと渡す。
- どちらも**地図経由**。地図が「正しい地図」なら、文脈窓が何であれ的確に渡せる。

---

## これが変える優先順位（前段の結論の固定）

| 下げる(使い捨ての足場) | 上げる(複利・取り返しのつかない) |
|---|---|
| ルールベース推論を作り込む | **捕捉の忠実度**(中身・全文・消費も含め完全に) |
| 攻めた破壊的フィルタ | **不変保存**＋ノード→領土の**back-pointer** |
| 「今 意味を出す」こと自体 | **MODE 軸**＋地図の**再生成/バージョン** |

---

## 捕捉忠実度 監査（territory は今しか取れない）

法則①の具体。今ロッシーに撮れている所を、取り返しのつかなさ順に:

| # | ロス | 取り返し不能度 | 安全に直せるか | 状態 |
|---|---|---|---|---|
| 1 | **screenText がウィンドウ“タイトル”だけ**(本文を撮らない) | **致命的** | AX API で本文取得=大きい/権限/負荷。要設計 | 未着手(最大の本丸) |
| 2 | **クリップボード上限**(コピー本文の切り詰め) | 高 | 低リスク | **済(5,000→40,000字)** |
| 3 | キーストロークのバッファ断片化(全文でなく塊) | 中 | 中 | 未着手 |
| 4 | ブラウザは**タイトル/URLのみ**(ページ本文なし) | 高 | 中(History.db/拡張) | 未着手 |
| 5 | 音声は再生メタ情報のみ(自分の発話の全文でない) | 中 | 大 | 範囲外(§3.6) |

原則:**撮らなかったものは未来の無限処理でも復元できない。** だから #1/#4 が次の本命投資。
整形・推論(地図側)は後でやり直せるので後回しでよい——逆に capture は今しか直せない。

## 系譜（この設計が辿り着いた道）

カバン持ち(吸収して主に還流)→ custode(所有でなく預かり手渡す)→ 「判断は批准で得る」→
「削るのは今のAI限界=現在形の妥協」→ **領土は永遠に・地図は作り直せる・意味は MODE で**。
批准は「あなたの評価」という*新しい信号を領土に記録する*手段として残る(永続要件ではない)。

*Capture everything, forever. Map it correctly, disposably. Mean it with mode.*
