# mull: Territory / Map / Mode（データ層の北極星）

> **信条**: 領土はロスなく永遠に。地図は正しく、指すだけ、作り直せる形で。
> 削らない。要約で原本を置き換えない。意味は「モード」で与える。

mull がエージェントに渡す材料であるための、データ層の一枚絵です。
これは構造の北極星です。視覚の側には対になる文書がありますが、そちらは新規投資を止めており
このリポジトリには入っていません。本書は凍結対象ではなく、むしろ位置づけが上がりました。

> **2026-08-08 の位置づけ更新**: mull の製品は GUI ではなく MCP サーフェスになりました
> （CLAUDE.md §5）。下図の右上「served at use-time」が MCP の13ツールそのもので、
> 本書の serving 層が製品の出口になりました。
> 「第二の脳に渡す」という言い方は「エージェントが引く」に置き換わりましたが、
> 5つの不変法則は一字も変わりません。

---

## 一枚絵

```
                         ┌──────── served at use-time ────────┐
                         │  small context → LENS (今日の小モデル用) │ ──▶ AI
                         │  large context → WHOLE map / territory │     (MCP / Copy to AI)
                         └─────────────────▲──────────────────────┘
                                           │  read-only · 使い捨て可 · 再生成可
   ┌──────────────────────────── MAP（地図 = 索引）────────────────────────────┐
   │   WHAT（何について）      ·   HOW（どう = MODE）   ·   NOW（今の文脈）        │
   │                                                                            │
   │   entity nodes ── tagged spans [ produce | consume | decide |              │
   │       │                          think | research | communicate ]          │
   │       │                                                                    │
   │       └── すべてのノード/スパンは下を「指す」（event id 参照）────────┐       │
   └───────────────────────────────────────────────────────────────────┼───────┘
                                                                         │
                       back-pointers（要約で置き換えない・原本へ戻れる）  │
                                                                         ▼
   ┌──────────────────────────── TERRITORY（領土 = _raw）──────────────────────┐
   │   immutable · lossless · complete · 絶対に消さない                          │
   │   keystrokes · clipboard · window · files · audio · browser · calendar      │
   │   ◀── 捕捉の忠実度だけが「今しか取れない」資産。処理は後でやり直せる。        │
   └────────────────────────────────────────────────────────────────────────────┘
```

---

## 5つの不変法則

1. **領土は不変、ロスレス、完全。絶対に消しません。** 捕捉の忠実度（タイトルではなく中身、
   断片ではなく全文）が、唯一「今しか取れない」資産です。2026年のあなたは再キャプチャできません。
2. **地図は領土を指す索引であって、置き換える要約ではありません。** どのノードも必ず
   生イベントを参照します。要約して原本を捨てる操作は禁止です。
3. **地図は使い捨てで、再生成可能で、バージョン付きです。** 今日の下手な地図はゼロコストです。
   無傷の領土から作り直せます。だから今ショボくても貯め続けて構いません。
4. **領土は時間に敏感で、地図は時間に鈍感です。** 投資は領土（捕捉）へ。
   整形や推論は地図側で、いつでも作り直せるので、賢さを作り込みすぎません。
5. **フィルタは読み出し時の差し替えレンズです。破壊的フィルタは存在しません。**
   今日の小さいモデル向けに見せ方を絞るだけです。レンズは未来に外せて、その下に完全な記録があります。

---

## 地図の3軸、そして欠けている決定打

| 軸 | 問い | 役割 | mull の現状（2026-08-08） |
|---|---|---|---|
| **WHAT** | 何について | entity / topic ノード（mull=Dream=アプリ、確定申告、この人） | `Entity.swift` あり。名寄せの解像度が低い（残課題） |
| **HOW = MODE** | どう語っているか | 姿勢とモードのタグ | ✅ `Mode.swift` と `ModeTests`。capture-time に付与される（下表の6値） |
| **NOW** | 今どこか | now-anchor | ✅ `CurrentState.swift`。MCP `whats_active_now` として公開済み |

> **更新**: 本書執筆時は「MODE 軸はほぼ無い。核心の欠落」と書きましたが、実装されました。
> 残る欠落は WHAT の名寄せ解像度と、下表「既存部品への対応」の back-pointer と再生成です。

### MODE 軸（どう語っているか）が一番効く

今日の失敗（税務の解説動画は仕事か遊びか、口述は思考かノイズか）は、全部 MODE が無いせいです。
消す代わりに、姿勢でタグ付けして意味を与えます。

| mode | 意味 | 例 |
|---|---|---|
| **produce** | 作った（著者はあなた） | コード、原稿、デザイン |
| **consume** | 受け取った（入力） | 記事、動画、音声 |
| **decide** | 選んだ、約束した | 方針転換、採否 |
| **think** | 考えた（独り言、口述、計画） | 喋りながらの製品思考 |
| **research** | 目的を持って調べた（consume に意図が乗ったもの） | 競合の FX 動画 |
| **communicate** | 伝えた | メール、投稿、会議 |

MODE はスパン（時間域やイベント塊）に付きます。削除ではなく、重み付け、選別、配置に使います。

> **⚠ 2026-08-09 まで、この「重み付け・選別」は実装されていませんでした。**
> `Mode` は捕捉時に計算して保存されていましたが、消費先は `ContextComposer`（配置）だけで、
> `Selection.rank`（選別）は mode を一度も読んでいませんでした。
> `Mode.weight` を追加して接続済みです（重みと測定上の限界は SELECTION-LAYER §4 が正本）。
> この節が「一番効く」と書いていた軸が、1年近く検索に効いていませんでした。
> 文書が実装より先に走ると起きる典型で、確認コストの低いものを確認しないまま書いた結果です。

共起で解けます。「税務の解説動画は consume だが、確定申告エンティティと共起するので research」
「長い口述は mull エンティティ下の think」。どちらも捨てずに、意味づけて置きます。

MODE 自体も再生成可能です（今日はルールとヒューリスティック、明日は LLM）。地図側なので作り直せます。

---

## mull の既存部品への対応（骨格はもう在る）

| 一枚絵の層 | 既存の実装 | 足りないもの |
|---|---|---|
| TERRITORY | `RawStore` と `_raw/<connector>/`（immutable source） | 捕捉の忠実度。ブラウザ本文、打鍵の断片化（下の監査 #3 と #4） |
| 設計全体図（schema） | `VaultLayout`（フラット構成） | territory / map / mode を映す形への整理 |
| MAP: WHAT | `Entity` と `Selection`（SELECTION-LAYER） | entity 解像度と名寄せ（Dream=mull）。最大の残課題 |
| MAP: NOW | ✅ `CurrentState`（now-anchor） | 無し |
| MAP: HOW | ✅ `Mode` と `Signal`（capture-time 分類） | mode の精度（今はルール、後で LLM に差し替え可） |
| back-pointer | 弱い | ノードから生イベント id への参照を一級にする |
| 再生成 | 無い | 地図のバージョン付き再生成（領土から rebuild） |
| serving | ✅ MCP 13ツール（製品の出口） | 小ctx はレンズ、大ctx は丸ごと、の切替 |

---

## 地図ノードの概念モデル（実装ではなく形）

```
MapNode {
  entity:   "mull"                      // WHAT（名寄せ済み）
  spans: [
    { range: 14:00–14:40,
      mode: .produce,                   // HOW
      refs: [evt#8841, evt#8842, …],    // ← TERRITORY を指す。要約で置換しない
      confidence: 0.7,
      mapVersion: "rules-v3" }          // ← 再生成で差し替わる
  ]
  now: false                            // NOW（anchor かどうか）
}
```

要点は、ノードは意味の置き場ですが本文は持たないことです。本文は領土にあり、`refs` で指すだけです。
だから地図を作り直しても領土は不変で、領土を読めば失われた文脈はいつでも戻ります。

---

## 再生成とバージョン（なぜ今ショボくていいか）

```
territory（不変）──▶ [extract pass]（ルール今 / LLM後）──▶ map vN（破棄可能）
                          ▲
                  AIが賢くなったら pass を差し替えて map vN+1 を再生成
```

- 地図は `mapVersion` を持ちます。抽出器を上げたら全地図を作り直します（territory は触りません）。
- 今日のウィンドウタイトル由来のゴミ project は `rules-v3` の地図です。未来に `llm-v1` で上書きします。
  間違った地図はコストになりません。

---

## 使う瞬間に渡す（serving）

- **小コンテキスト（今日の ChatGPT）**にはレンズで地図のスライスを渡します。
  レンズは破壊せず、差し替えられます。
- **大コンテキスト（未来、接続エージェント）**には地図ごと、必要なら領土ごと渡します。
- どちらも地図経由です。地図が正しければ、文脈窓が何であれ的確に渡せます。

---

## これが変える優先順位

| 下げる（使い捨ての足場） | 上げる（複利で効く、取り返しのつかないもの） |
|---|---|
| ルールベース推論を作り込む | 捕捉の忠実度（中身、全文、消費も含め完全に） |
| 攻めた破壊的フィルタ | 不変保存と、ノードから領土への back-pointer |
| 「今 意味を出す」こと自体 | MODE 軸と、地図の再生成とバージョン |

---

## 捕捉忠実度の監査（territory は今しか取れない）

法則1の具体です。今ロッシーに撮れている所を、取り返しのつかなさ順に並べます。

| # | ロス | 取り返し不能度 | 安全に直せるか | 状態 |
|---|---|---|---|---|
| 1 | screenText がウィンドウのタイトルだけで、本文を撮らない | **致命的** | AX API での本文取得は規模、権限、負荷が大きい。要設計 | 未着手（最大の本丸） |
| 2 | クリップボード上限（コピー本文の切り詰め） | 高 | 低リスク | ✅ 済（5,000字から40,000字へ） |
| 3 | キーストロークのバッファ断片化（全文ではなく塊） | 中 | 中 | 未着手 |
| 4 | ブラウザはタイトルと URL のみで、ページ本文が無い | 高 | 中（History.db や拡張） | 未着手 |
| 5 | 音声は再生メタ情報のみで、自分の発話の全文ではない | 中 | 大 | 範囲外 |

原則として、撮らなかったものは未来の無限処理でも復元できません。だから #1 と #4 が次の本命投資です。
整形や推論（地図側）は後でやり直せるので後回しで構いません。capture だけは今しか直せません。

## 系譜（この設計が辿り着いた道）

カバン持ち（吸収して主に還流）から custode（所有せず預かって手渡す）へ、
そして「判断は批准で得る」「削るのは今の AI 限界であって現在形の妥協」を経て、
「領土は永遠に、地図は作り直せる、意味は MODE で」に辿り着きました。
批准は「あなたの評価」という新しい信号を領土に記録する手段として残ります（永続要件ではありません）。

*Capture everything, forever. Map it correctly, disposably. Mean it with mode.*
