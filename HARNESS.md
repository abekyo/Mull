# HARNESS — mull が生成すべきものの仕様と、それを生む訂正ループ

> **由来**: 2026-08-09、既存の高品質ハーネス2本（`mailmagazine-creator` / `research-protocol`）を
> 解体して設計原理を抽出した。**あれが mull の出力の到達目標**である。
>
> **本書の2部構成**:
> - **第I部** — 良いハーネスを成立させている8原理。**mull が生成した成果物を検査する基準**
> - **第II部** — その成果物を生む訂正ループ。**Case Card スキーマと自動化の境界**
>
> 位置づけ: CLAUDE.md §7.3（訂正ループ＝堀）と SELECTION-LAYER §5（学習ループ）の実装仕様。
> STRATEGY の「売り物を配管からハーネスへ」を成立させるための単一ソース。

---

# 第I部 — ハーネス設計の8原理

参照した実物: `mailmagazine-creator`（8 Phase・9 Principle・9 Case・19ラベル）と
`research-protocol`（3モード・5層・反証専任）。以下は両者から共通して抽出した構造。

## 原理1. モデルの失敗様式を名指しして、構造で殺す

**普通の指示書**: 「Xをやれ」
**良いハーネス**: 「AIはなぜXをやらないか」を書き、**やらざるを得ない構造**を置く

実例:

| 名指しされた失敗様式 | 置かれた構造 |
|---|---|
| AIは生成原理上、フェーズ数が多いほど省略したくなる | Phase完了表 ＋ 各✓に実体成果物を要求 |
| 「自己評価式」のチェックは通したくなる | 「OK」「該当なし」の**単独記述を禁止** |
| AIは整合性を追求するので矛盾が薄まる | 「矛盾はバグではなく最大の武器」と明示 |
| AIは成功パターンを表面マッチングで再利用する | PRC-003 文脈転写の不可能性 |
| 1ソース流し読みで「調べました」とする | 反証専任サブエージェントを**独立させる** |

> **これは指示書ではなく敵対的設計である。** 書き手はモデルの挙動を予測し、
> その予測が外れる方向に構造を置いている。

**mull への適用**: 生成するハーネスには、**そのユーザーの作業でエージェントが繰り返し失敗する様式**を
書く。それは mull が観測できる——同じ訂正が繰り返されている箇所が失敗様式そのもの。

## 原理2. 実体的根拠要求 — 自己申告を成果物に変換する

単体で最もレバレッジが高いルール。

```
✗  立場保全ゲート：該当なし

✓  立場保全ゲート：該当なし
   - 走査した語例：「胃にきた」(0回)「しんどい」(0回)「もっさり」(0回)
   - 走査したパラフレーズ候補：「○○期間しんどかった」「△△に揺さぶられた」(該当ゼロ)
   - 結論：契約2違反ゼロ
```

さらに `mailmagazine-creator` は
**「語句系チェックは自己評価ではなく grep で行い、走査パターンと出力を根拠として提示」**
まで降りている。

> **ハーネスの品質 = 事後検証可能性。** 「やった」と書けてしまうチェックは、
> 数回で必ず形骸化する。

**mull への適用**: `eval/run.sh` と CI は既にこの思想の実装。ハーネス生成側にも同じ規律を要求する。

## 原理3. 矛盾調停の優先順位を明示する

ハーネスに複数の圧（最適化目標）があると、**衝突時にモデルが恣意的に解決する**。
だから優先順位を書く。

```
Position Contract が常に勝つ（ドラマ性・読み物性・カジュアルさよりも上位）
```

さらに5つの圧それぞれについて調停表を作っている:

| 最適化圧 | 立場保全との調停 |
|---|---|
| 連載性 | 前回接続・伏線はOK。認識が動く主人公アークはNG |
| 代償のある雑談 | 過去の代償・自分の能力不足はOK。外部要因への現在進行形被害感はNG |
| 感情の漏れ | 自己ツッコミ・過去自虐はOK。「しんどい」「気づいた」はNG |

**mull への適用**: 生成ハーネスにも必ず「何が何に勝つか」を書く。
mull 本体の序列（STRATEGY > DIRECTION > CLAUDE.md）はこの原理の適用例。

## 原理4. 単一ソースと、許される重複の線引き

「重複禁止」ではなく **「完全一覧の重複だけが禁止」** まで書いてある:

> 禁止表現の正本は knowledge-base の表。style-guide・evaluation 側には
> **高頻度の代表例だけ**置いてよいが、必ず「全件は knowledge-base 参照」と明記し、
> **完全一覧として扱わない**（完全一覧の二重管理がドリフトの原因になる）

`phase-discipline.md` はさらに、**なぜ単一ソースなのか**を自分で説明している——
「各スキルが個別に書くとスキルごとにドリフトする」。

**mull への適用**: 生成ハーネスがユーザーの既存 `CLAUDE.md` / `AGENTS.md` / skills と
重複したとき、**どちらが正本かを宣言させる**。宣言なき重複は必ずドリフトする。

## 原理5. 二層蓄積 — 判断イベント → 判断軸

```
Case Card（9セクション）    1件の判断イベント
        ↓  複数ケースから共通原理が見えたら（1件では作らない）
Principle Card              判断軸
```

核心はこの2行:

> **保存単位は「ルール」ではなく「判断イベント」**
> **成功パターンは単体で保存しない。「この条件だから効いた」を必ず記録する**

**mull への適用**: 第II部がこの実装仕様。

## 原理6. 各手順に「省略するとどうなるか」を書く

`mailmagazine-creator` は全 Phase に **なぜ必要か / 省略するとどうなるか / アンチパターン** を持つ。

> Phase 1 を省略すると → 口述を要約してから Phase 2 に入ることになり、
> 矛盾・逡巡・感情が「整理」されて消滅する。整理された素材からは説明文しか生まれない。

**理由が書いてあるルールは、書かれていない状況にも汎化する。** 結論だけのルールは汎化しない。

## 原理7. Progressive disclosure

SKILL.md が入口で、`references/` は Phase ごとに読む。`読み込み指示` 表が
**Phase → ファイル → 目的** を対応させている。全部を最初に読ませない。

`research-protocol` は同じことを**規模モード**（クイック/標準/フル）でやっている——
判断の重さで工程量を変える。

**mull への適用**: 3層コンテキスト（me/now/full）は既にこの形。ハーネスも同じ形にする。

## 原理8. ルールに出所と日付を付ける

禁止表現テーブルには**発見日**が並ぶ（2026-03-11 / 04-01 / 04-02 / 04-26 / 07-08）。
Case Card には `Date` と `Retrieval cues`。Principle Card には `Derived from`。

**出所のないルールは、腐っても誰も気づけない。**

**mull への適用**: 生成ハーネスの各行に **provenance**（どの記録から来たか）を付ける。
これは Curator が既に持っている機構で、CLAUDE.md §7.1 の
「この行はこの記録から来た、と示せるものだけ」と同じ要求である。

---

## 第I部の要約 — 検査表

mull が生成したハーネスは、以下で検査する。

| # | 検査項目 | 満たさない場合の症状 |
|---|---|---|
| 1 | エージェントの失敗様式が名指しされているか | 「Xをやれ」の羅列 → 数回で無視される |
| 2 | 各チェックが実体成果物を出すか | 「OK」で埋まる → 形骸化 |
| 3 | 圧の優先順位が書かれているか | 衝突時にモデルが恣意的に解決 |
| 4 | 正本が宣言されているか | 既存 CLAUDE.md とドリフト |
| 5 | 判断イベントとして保存されているか | 条件が変わると誤用される |
| 6 | 「省略するとどうなるか」があるか | 汎化しない |
| 7 | 入口と参照が分離されているか | コンテキストを食い潰す |
| 8 | 各行に出所があるか | 腐敗が検出できない |

---

# 第II部 — 訂正ループの実装仕様

## 1. なぜ「編集距離」では足りないか

現在 mull が持っている訂正の記録は:

- **Curator の provenance**（agent / human / pinned）— 誰が書いたか
- **`EditDistance` / `ReportWriter.fidelitySeries`** — どれだけ直されたか

つまり**訂正を「量」として持っている**。42% → 18% → 7% という収束曲線は出せる。

しかし PRC-003（文脈転写の不可能性）が言っているのは:

> 成功した表現は「成功した文脈」とセットでしか再利用できない。
> 表現だけ抜き出して別の文脈に移植すると意味が変わる。

**条件を捨てた訂正は、次に誤用される。** 編集距離からは
「何が同じで何が違ったか」が復元できない。

実例: Case MLG-2026-04-26-001 の境界——

> 「胃」という同じ単語でも、文脈で自虐成立/不成立が変わる。
> 「胃まだ無事です」「胃が痛い（過去）」= OK、「胃にきた（現在の停滞期間）」= NG

**この境界は diff からは出てこない。** 訂正は「構造」で持つ必要がある。

## 2. Correction Card — mull の訂正記録スキーマ

Case Card の9セクションを、mull が埋められるもの / 埋められないもので割る。

| # | セクション | 埋める主体 | mull の材料 |
|---|---|---|---|
| 1 | **Context snapshot**<br>目的 / 読み手 / 発信者の立場 / 守るべきもの | **mull（自動）** | `whats_active_now()` のスナップショット。訂正が起きた時刻の active entity / session / 直近の高salience行動 |
| 2 | **Failed move**<br>agent が出したもの / 人間の訂正 / 表層の問題 | **mull（自動）** | Curator の provenance 付き diff。`agent` ブロック → `human` ブロックへの変化 |
| 3 | **Signals observed**<br>違和感の信号 | **mull（自動）** | 機械走査。削除された語の頻度、追加された語、`contentType` / `mode` の分布変化 |
| 4 | **Hidden axis**<br>真の分水嶺 | **人＋モデル** | ❌ ログからは出ない |
| 5 | **Reasoning chain**<br>読み手の認知プロセス | **人＋モデル** | ❌ ログからは出ない |
| 6 | **Judgment criteria**<br>本人の判断基準 | **人** | ❌ |
| 7 | **Reusable rule**<br>Use when / Avoid when / Check question | モデルが草案、人が承認 | 4〜6 から導出 |
| 8 | **Contrast**<br>似ているが違う成功例、境界 | **人＋モデル** | ❌ ただし mull は「似ている過去のケース」を `search` で提示できる |
| 9 | **Transfer**<br>抽象原理 / スコープ / タグ | モデルが草案、人が承認 | — |

### 自動化の境界（これが設計の核心）

> **1–3 は自動化する。4・5・8 は自動化しない。**

4・5・8 こそがハーネスを他のプロンプト集と分けている部分であり、
**そこを自動化しようとした瞬間、生成されるのは「もっともらしい嘘」になる**。

これは CLAUDE.md §3.6 の分身の憲法「**下書きまで・発火は常に主**」と、
偶然ではなく必然的に一致する。**mull が出せるのは材料と草案で、判断軸は人が引く。**

そして DIRECTION 付録A の epistemics ②「解釈ではなく観測」とも一致する——
1–3 は観測、4–6 は解釈。**観測は自動化してよく、解釈は人間の仕事。**

## 3. 足りない3つの機構

| 足りないもの | 何のために要るか | 実装の当たり |
|---|---|---|
| **Correction Card のスキーマ** | 訂正を量ではなく構造で持つ | Curator の書き戻し時に、diff だけでなく §2 の 1–3 を `~/mull/06_knowledge/corrections/` に生成する |
| **Retrieval cues** | 蓄積したものが**見つかる**設計 | Card に `entity` / `contentType` / タグを付け、`Selection.rank` の対象に入れる。`search` で引ける |
| **Check question** | 次回の生成時にこの判断を**呼び出すトリガー** | セクション7の `Check question` を、ハーネス生成時のプリフライト検査に流す |

`Retrieval cues` が無いと蓄積は死蔵する。`Check question` が無いと蓄積は次回に効かない。
**Case Card が9セクションある理由の半分は、この2つのためである。**

## 4. ループの全体像

```
     mull がハーネスを草案する（第I部の8原理に従って）
                    ↓
     人が直す ─────────────────────────────┐
                    ↓                       │
     Curator が provenance 付きで保存        │
                    ↓                       │
     Correction Card 1–3 を自動生成         │
                    ↓                       │
     人＋モデルが 4–9 を埋める（＝解釈）     │
                    ↓                       │
     Retrieval cues が付いて索引される       │
                    ↓                       │
     次回の草案時、Check question として発火 ┘
```

### 測るもの

| 指標 | 何を示すか | 実装 |
|---|---|---|
| **編集距離の収束**（42% → 7%） | あなたに近づいているか | ✅ `EditDistance` で今日から取れる |
| **Check question のヒット率** | 蓄積が次回に効いているか | Card の `Check question` が発火して修正を止めた回数 |
| **同一 Hidden axis の再発** | 学習しているか | 同じ軸の Card が2回目に出たら、ハーネスへの反映が失敗している |

> **編集距離だけでは「近づいた」しか言えない。**
> **Check question のヒット率と再発率が「学習した」の証拠になる。**

## 5. Principle への昇格ルール（1件では作らない）

`mailmagazine-creator` の規律をそのまま採る:

- Principle Card は **複数の Case から共通原理が見えた時だけ**作る
- 1件からの一般化は禁止（アンチパターン「過剰一般化: 1回の修正から広すぎるルールを導出する → 別の文脈で邪魔になる」）
- Principle には `Derived from` で元の Case を書く

## 6. これがなぜ mull にしかできないか

| | 訂正の量 | 訂正の**文脈**（何をしていた時か） |
|---|---|---|
| Screenpipe / ManicTime / Timing | — | — |
| Copilot Memory | リポジトリの成果物から推定 | コミット前の試行は見えない |
| Mem0 / Zep / Letta | アプリが渡した分だけ | OSレベルの行動は見えない |
| **mull** | **Curator の provenance** | **`whats_active_now()` のスナップショット** |

Correction Card のセクション1（Context snapshot）を**自動で埋められるのは mull だけ**である。
他は「何を直したか」は取れても「**何をしている最中に直したか**」が取れない。

STRATEGY §2-2 の「唯一の非対称性」は、ここで具体的な機構になる。

---

## 付録. 実物への参照

本書が解体した2本は `~/.claude/skills/` に導入済み:

| | 構成 | 特に学ぶべき点 |
|---|---|---|
| `mailmagazine-creator` | 8 Phase / 4契約 / 19ラベル / 9 Principle / 9 Case / 18サンプル | 原理1・2・3・5（敵対的設計と二層蓄積の完成形） |
| `research-protocol` | 3モード / 5層 / 探索設計 / 反証専任 / 証拠台帳 / 飽和判定 | 原理1・7（反証を独立させる、規模で工程を変える） |

`references/phase-discipline.md` は
**「Phase 体系の制作フロー型スキル専用。別の体系には思想だけを内蔵せよ」**
と自ら適用範囲を限定している。ハーネスを増やす時はこの指示に従うこと——
**構造をコピーするのではなく、思想を内蔵する。**
