# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
build/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.swp
*~

# Xcodegen (regeneratable)
# Mull.xcodeproj is generated from project.yml (glob also catches stray "Mull 2.xcodeproj")
Mull*.xcodeproj/

# Claude Code local state
.claude/

# Machine-specific signing identity (see Local.xcconfig.example). Signing.xcconfig
# is the tracked one project.yml points at; it #include?s this file when present.
Local.xcconfig

# Internal business documents — deliberately NOT published (decided 2026-08-09).
# The technical docs (DIRECTION / SELECTION-LAYER / MAP-ARCHITECTURE / ROADMAP)
# ship with the repo; these two hold the live commercial plan and the competitor
# revenue ledger it rests on. Docs that reference them use plain text, not links,
# so nothing dangles in the published tree.
STRATEGY-2026-08.md
MARKET-2026-08.md

# Build artifacts that land in the repo root
default.profraw
